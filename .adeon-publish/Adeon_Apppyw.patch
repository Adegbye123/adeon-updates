--- a/Adeon App.pyw
+++ b/Adeon App.pyw
@@ -603,7 +603,7 @@
 # published version; updates are full program-file releases, not incremental
 # patches.  User data (Chrome Data, reports, logs, local state) is never part of
 # an update package and is therefore preserved.
-ADEON_VERSION = "1.0.57"
+ADEON_VERSION = "1.0.58"
 UPDATE_FEED_FILE = BASE / "Update Feed URL.txt"
 OFFICIAL_UPDATE_FEED = "https://raw.githubusercontent.com/Adegbye123/adeon-updates/main/adeon/manifest.json"
 UPDATE_HELPER = BASE / "Adeon Update Helper.py"
@@ -979,8 +979,15 @@
         },
     )
     try:
-        with urllib.request.urlopen(req, timeout=2.5) as resp:
-            data = resp.read(1024 * 1024 + 1)
+        try:
+            with urllib.request.urlopen(req, timeout=2.5) as resp:
+                data = resp.read(1024 * 1024 + 1)
+        except Exception as first_error:
+            # The user's Windows log repeatedly showed urllib TLS/read timeouts even
+            # though the public GitHub feed itself was live. Use Windows curl as an
+            # immediate alternate transport for the lightweight background probe.
+            update_diag(f'FAST MANIFEST urllib failed; trying curl | url={url} error={first_error}')
+            data = _curl_public_https(request_url, 4.0, 1024 * 1024)
         if len(data) > 1024 * 1024:
             raise RuntimeError('Update manifest exceeded the allowed size')
         obj = json.loads(data.decode('utf-8-sig'))
@@ -2113,7 +2120,7 @@
             lr_window_open = bool(lr_hwnd)
             lr_seen = bool(bridge_live or lr_window_open)
 
-            # v1.0.57: Picker AND Labels use the browser-independent Screen Vision
+            # v1.0.58: Picker AND Labels use the browser-independent Screen Vision
             # feed. Browser Integration is no longer a prerequisite for either mode.
             # Labels are rendered by Adeon as Windows overlay controls and keep their
             # existing persistent/cross-device sync data outside the browser.
